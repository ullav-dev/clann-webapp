// Root layout: html/body are provided by the [locale] child layout.
// This wrapper is required by Next.js App Router.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
