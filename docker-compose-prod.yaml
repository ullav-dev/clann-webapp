services:
  webapp:
    image: ghcr.io/ullav-dev/clann-webapp:latest
    container_name: clann-webapp
    init: true
    env_file: .env.prod
    networks:
      - ullav-net
    restart: unless-stopped
    ports:
      - "3002:3002"

networks:
  ullav-net:
    external: true
